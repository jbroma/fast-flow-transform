{
  "name": "@fft-tests/rolldown",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rolldown -c rolldown.config.mjs",
    "e2e": "vitest --config vitest.config.ts run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "fast-flow-transform": "workspace:*",
    "rolldown": "1.0.0-rc.7",
    "vitest": "^4.0.16"
  }
}
